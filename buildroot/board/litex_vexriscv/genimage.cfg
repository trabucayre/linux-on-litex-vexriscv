image boot.vfat {
	vfat {
		files = {
			"opensbi.bin",
			"Image",
			"rootfs.cpio"
		}
	}

	size = 50M
}

image sdcard.img {
	hdimage {
	}

	partition boot {
		partition-type = 0xC
		bootable = "true"
		image = "boot.vfat"
